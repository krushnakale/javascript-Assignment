
// the following statement executes as soon as the script file referenced by the scripts tag in html file loads in the browser .
// docment.getElementById("heading").innerHTML = "hello world";

// to have things work on button click, we do not want these to run when browser loads the script
// it should executes some time later when buttons gets clicked;
function handleButtonClick() {
    document.getElementById("")
}